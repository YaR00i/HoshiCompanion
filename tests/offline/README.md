# Local workspace checks

Use `python tools/dev.py check` with the actual Godot engine.
Optional offline fixture scripts and the illustrative IK image from the downloadable 0.2 archive were not transferred to this workspace.
The main runtime test and all application scripts match that archive.
